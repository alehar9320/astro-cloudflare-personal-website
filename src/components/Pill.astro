---
import { getEntry } from 'astro:content';

const flagsEntry = await getEntry('flags', 'config');
const isPillGradientEnabled = flagsEntry?.data.enable_pill_gradient_v1 ?? false;
---

<div class={`pill ${isPillGradientEnabled ? 'gradient-shift' : ''}`}><slot /></div>

<style>
  .pill {
    display: flex;
    padding: 0.5rem 1rem;
    gap: 0.5rem;
    color: var(--accent-text-over);
    border: 1px solid var(--accent-regular);
    background-color: var(--accent-regular);
    border-radius: 999rem;
    font-size: var(--text-md);
    line-height: 1.35;
    white-space: nowrap;

    /* Interactive state base styles */
    background-image: linear-gradient(
      110deg,
      var(--accent-regular) 0%,
      var(--gradient-stop-1) 50%,
      var(--gradient-stop-3) 100%
    );
    background-size: 250% 100%;
    background-position: 0% 50%;
    box-shadow: var(--shadow-sm);
  }

  .pill.gradient-shift {
    transition:
      background-position 0.3s ease,
      box-shadow 0.3s ease;
  }

  .pill.gradient-shift:hover {
    background-position: 100% 50%;
    box-shadow: var(--shadow-md);
  }

  @media (prefers-reduced-motion: reduce) {
    .pill.gradient-shift {
      transition: none;
    }

    .pill.gradient-shift:hover {
      background-position: 0% 50%;
    }
  }
</style>
