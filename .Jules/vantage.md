## 2026-04-08 - Enforcing Mandatory Alt Text and Increasing Release Coverage | **Learning:** Accessibility must be enforced at the schema level to prevent regression, and API error states often represent significant coverage gaps. | **Action:** Removed `.optional()` from `img_alt` in `src/content.config.ts` and added a test case for non-OK GitHub API responses in `src/__tests__/github-releases.test.ts`.

## 2026-04-09 - Improving Test Coverage and Silencing Compiler Hints | **Learning:** Comprehensive testing of environment variable fallbacks and optional fields is essential for robust utilities, and addressing compiler hints keeps the build logs clean and actionable. | **Action:** Added unit tests for fallback branches in Sentry configs and optional fields in `github-releases.ts`; added `is:inline` to JSON-LD script in `about.astro` to resolve Astro compiler hint.
