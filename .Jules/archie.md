2026-03-30 - Architecture Governance Baseline & Edge-First Directives
Learning: Standardizing ADR formats with explicit 'Directives for AI Agents' and establishing a Repository Tech Radar prevents AI agent drift across edge runtime constraints (Cloudflare Workers vs Render Node test env) and eliminates redundant dependencies.
Action: Require all future ADRs to define actionable 'Do' and 'Don't' directives for AI agents and maintain the master index in docs/adr/README.md.
