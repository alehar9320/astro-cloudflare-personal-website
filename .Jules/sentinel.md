# Sentinel Journal

## 2026-04-07 - Security Hardening & XSS Mitigation | **Vulnerability:** Moderate vulnerability in `yaml` (Stack Overflow) and unsafe `set:html` usage in `Hero.astro` tagline. | **Prevention:** Updated `@astrojs/check` to patch dependencies, refactored `Hero.astro` to use named slots for HTML content, and implemented a Defense-in-Depth Content Security Policy (CSP).
