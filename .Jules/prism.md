## 2026-03-30 - Interactive Aurora Wave Refraction MVP | Signal: Northern Lights Theme Enhancement | Lean Implementation: Isolated in src/pages/lab/aurora-refraction.astro & src/utils/aurora-math.ts

- **Garbage Collection & Memory Thresholds:** Capped DPR scale at 2.0 max via `getOptimizedDpr()` utility to limit canvas backing store size on high-DPI displays. Used single persistent path iteration per layer (`ctx.beginPath()`) with dynamic step sizing based on width resolution (`Math.max(2, Math.floor(width / 200))`), avoiding zero-allocation GC spikes in `requestAnimationFrame`.
- **Math Logic Setups:** Implemented cubic smoothstep attenuation `(1 - distance / radius)^2` for fluid light refraction distortion when interacting with pointer coordinates. Combined multi-frequency sine wave harmonics `computeAuroraWave(x, t)` for organic wave dynamics.
- **Mobile Viewport Scaling Strategies:** Built responsive container bounds matching parent width and fixed aspect ratios. Integrated `prefers-reduced-motion` check to halt continuous rendering loop while preserving static rendered aurora wave vectors. Ensured Astro client-side page lifecycle teardown on `astro:before-swap` event by canceling animation frame request and unbinding pointer listeners.
